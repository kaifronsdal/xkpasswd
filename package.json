{
  "name": "xkpasswd-firefox-extension",
  "version": "1.0.0",
  "description": "Firefox browser extension for generating secure, memorable passwords using the XKPasswd algorithm",
  "scripts": {
    "build": "webpack --mode=production",
    "build:dev": "webpack --mode=development",
    "watch": "webpack --watch --mode=development",
    "clean": "rm -rf dist build",
    "lint": "echo 'No linting configured'",
    "test": "echo 'No tests configured'",
    "dev": "npm run clean && npm run watch"
  },
  "keywords": ["firefox", "extension", "password", "generator", "xkpasswd", "security"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "xkpasswdjs": "github:bartificer/xkpasswd-js"
  }
}
